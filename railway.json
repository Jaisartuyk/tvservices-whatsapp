{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && python3 manage.py collectstatic --noinput && gunicorn tvservices.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "production": {
      "variables": {
        "DJANGO_SETTINGS_MODULE": "tvservices.settings",
        "PYTHONPATH": ".",
        "PORT": "$PORT"
      }
    }
  }
}
